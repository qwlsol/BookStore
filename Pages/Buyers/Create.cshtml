@page
@model BookStore.Pages.Buyers.CreateModel
@{
    ViewData["Title"] = "Добавить покупателя";
}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-success">
                <div class="card-header bg-success bg-opacity-10 border-success">
                    <h3 class="mb-0 text-success">Добавить покупателя</h3>
                </div>
                <div class="card-body">
                    <form method="post">
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Фамилия</label>
                            <input asp-for="Buyer.LastName" class="form-control" />
                            <span asp-validation-for="Buyer.LastName" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Email</label>
                            <input asp-for="Buyer.Email" class="form-control" />
                            <span asp-validation-for="Buyer.Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Телефон</label>
                            <input asp-for="Buyer.Phone" class="form-control" />
                            <span asp-validation-for="Buyer.Phone" class="text-danger small"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success">Сохранить</button>
                            <a asp-page="Index" class="btn btn-outline-secondary">Отмена</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}